/**
 * @deprecated Prefer `/accounts` (`pages/Accounts.tsx`).
 * Kept so old bookmarks to /wallets still work via App redirect + this re-export.
 */
export { Accounts as Wallets } from '@/pages/Accounts'
